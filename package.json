{
  "name": "content-analytics-platform",
  "version": "1.0.0",
  "description": "AI-powered content performance analyzer for social media creators",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["mern", "analytics", "ai", "social-media", "content-creation"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
